// Includes
let numbers = [1, 3, 4, 7, 9, 11];
if(numbers.includes(7)) {
    console.log('Si se encuentra el 7');
} else {
    console.log('No se encuentra');
}

// Potencia
let base = 4;
let exponent = 3;
let result = base ** exponent;
console.log(result);